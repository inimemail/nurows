Changed branch/field
- Branch: main (working tree only; no commit created).
- Probe target fields: checkRounds and attemptsPerRound are configurable per target, default to 3, and are clamped to 1-10.
- Probe execution: every round shares the configured timeout; any successful attempt ends the check, while every configured attempt must fail before a failed report is accepted.
- Probe aggregation: all assigned probes must submit fresh, complete evidence for the target's configured rounds and attempts before failover is eligible.
- Probe upgrade: agent version is 1.2.0; rerunning the generated install/upgrade command downloads the current agent and restarts nurossh-probe.service while reusing the long-lived registration token.
- UI: target create/edit dialog exposes check rounds and attempts per round; target rows show the configured dimensions; probes below 1.2.0 show an upgrade action.

Artifacts
- MODIFIED_FILE: /Users/inim/nurows/.codex-artifacts/MODIFIED_FILE
- DIFF_FILE: /Users/inim/nurows/.codex-artifacts/DIFF_FILE
- VERIFICATION.txt: /Users/inim/nurows/.codex-artifacts/VERIFICATION.txt
- ROLLBACK.sh: /Users/inim/nurows/.codex-artifacts/ROLLBACK.sh

BASELINE
Command: npm test && python3 -m unittest discover -s test -p 'test_probe_agent.py' -v && npm run build && node --check server/orchestration.js && node --check server/index.js && python3 -m py_compile probe-agent/nurossh_probe.py && bash -n install.sh probe-agent/install.sh probe-agent/uninstall.sh && docker compose config >/dev/null
Input: independent git-archive copy of HEAD before the configurable-check and probe-upgrade working-tree changes, with the workspace node_modules linked read-only for the build.
Literal result: Node tests 34, pass 34, fail 0; Python probe tests 3, OK; vite build succeeded; Node syntax, Python compilation, shell syntax, and Docker Compose config passed.
Exit status: 0

MODIFIED
Command: npm test && python3 -m unittest discover -s test -p 'test_probe_agent.py' -v && npm run build && node --check server/orchestration.js && node --check server/index.js && python3 -m py_compile probe-agent/nurossh_probe.py && bash -n install.sh probe-agent/install.sh probe-agent/uninstall.sh && docker compose config >/dev/null && git diff --check
Input: /Users/inim/nurows current working tree.
Literal result: Node tests 34, pass 34, fail 0; Python probe tests 4, OK; vite build succeeded; Node syntax, Python compilation, shell syntax, Docker Compose config, and git diff check passed. Vite emitted only its existing 500 kB chunk-size warning.
Exit status: 0

ROLLBACK
Command: /Users/inim/nurows/.codex-artifacts/ROLLBACK.sh /Users/inim/nurows/.codex-artifacts/ROLLBACK_TEST_COPY /Users/inim/nurows/.codex-artifacts/BASELINE_FILE
Input: independent copy of modified server/orchestration.js and the preserved HEAD version of that file.
Literal result: restored baseline hash on independent copy
Exit status: 0

Restored behavior/status: ROLLBACK_TEST_COPY matches BASELINE_FILE byte-for-byte and restores fixed 3x3 target validation for the selected rollback artifact; the working tree remains modified with configurable checks and probe version 1.2.0.
